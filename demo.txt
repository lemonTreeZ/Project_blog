版本控制系统

集中式和分布式（CVS和SVN）

集中式：版本库集中放在中央服务器，特点是保密性特别强，但是需要联网进行，中央服务器出错，则代码全部炸掉

分布式：没有中央服务器，每个人的电脑都是一个完整的版本库（比特币），分支管理

cmder中命令行才是我们的操作系统，该应用是为了在windows操作系统中实现linux命令。

进入盘符：cd 盘符名 / （E:/）

进入某个目录下： cd 文件夹\

git init：将当前目录设置为git服务

初始化文件夹后，会在当前目录产生一个.git的隐藏文件夹，命令行后面会跟上一个(master命令)

表明进入了一个已经启动了git服务的目录

git是一个源码管理工具，只能对文本文档进行管控，图片和视频无法对它进行管控

git status命令：当前系统git的一个状态

git add：添加当前文件

git remove：删除当前文件

git commit -m  "提示信息" ：提交文件

add 和commit命令的区别：

首先需要知道三个概念：工作区（Working Directory）、版本库（Repository）、暂存区（Stage or index）

####工作区

当开发一个项目时，主目录就是你的工作区

####版本库

工作区中有一个隐藏项目.git,这个就是git的版本库了

####暂存区

Git版本库里面存了很多文件，其中包括称为Stage或者index的暂存区，还有一个git为我们自动创建的第一个分支master,以及指向master,以及指向master的一个指针HEAD,所以，现在，git commit就是往master分支上提交更改。

​	所以git add和git commit 的区别就在于：git add把文件添加进去，实际上就是把文件修改添加到暂存区，git commit 提交更改，实际上就是把暂存区的所有内容提交到当前分支。因为我们创建Git版本库时，Git自动为我们创建了唯一的一个master分支。所以，git commit就是往master分支上提交更改。

​	可以简单的理解为，需要提交的文件修i该通通放在暂存区，然后一次性提交暂存区的所有修改，所以要想将修改提交到master中，一定要先git add到暂存区中，再git commit到master分支。

​	所以可以多次add不同的文件

设置提交信息













